import 'package:flutter/material.dart';

class UpdateTaskView extends StatelessWidget {
  static const id = '/update-task-view';

  const UpdateTaskView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
